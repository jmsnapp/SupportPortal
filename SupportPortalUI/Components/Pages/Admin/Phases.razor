@page "/admin/phases"
@using SupportPortalDomain.Models
@inject SupportPortalUI.ApiClients.Interfaces.IPhasesApiClient Clients

<PageTitle>Admin - Phases</PageTitle>

<h3>Phases</h3>

<div class="mb-3">
    <button class="btn btn-primary" @onclick="() => showCreate = true">Create Phase</button>
</div>

@if (isLoading)
{
    <Loading />
}
else if (!string.IsNullOrEmpty(error))
{
    <ErrorMessage Message="@error" />
}
else
{
    <table class="table table-sm">
        <thead><tr><th>Id</th><th>Name</th><th>Deleted</th><th></th></tr></thead>
        <tbody>
            @foreach (var c in items)
            {
                <tr>
                    <td>@c.Id</td>
                    <td>@c.Name</td>
                    <td>@c.Deleted</td>
                    <td>
                        <button class="btn btn-sm btn-secondary me-1" @onclick="() => Edit(c)">Edit</button>
                        <button class="btn btn-sm btn-danger" @onclick="() => ConfirmDelete(c)">Delete</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

<AdminEntityModal IsOpen="showCreate" IsOpenChanged="@(b => showCreate = b)" Title="Create Phase" Model="newModel" OnSaved="OnCreateSaved" />
<AdminEntityModal IsOpen="showEdit" IsOpenChanged="@(b => showEdit = b)" Title="Edit Phase" Model="editModel" OnSaved="OnEditSaved" />
<ConfirmModal IsOpen="showConfirm" IsOpenChanged="@(b => showConfirm = b)" Title="Delete Phase" Message="Are you sure you want to mark this phase as deleted?" OnConfirmed="OnDeleteConfirmed" />

@code {
    private IEnumerable<Phase> items = Array.Empty<Phase>();
    private bool isLoading = true;
    private string? error;

    private bool showCreate;
    private bool showEdit;
    private Phase newModel = new();
    private Phase editModel = new();

    protected override async Task OnInitializedAsync()
    {
        await LoadAsync();
    }

    private async Task LoadAsync()
    {
        isLoading = true;
        error = null;
        try
        {
            items = await Clients.GetAllAsync();
        }
        catch (Exception ex)
        {
            error = ex.Message;
        }
        finally { isLoading = false; }
    }

    private void Edit(Phase dto)
    {
        editModel = new Phase { Id = dto.Id, Name = dto.Name, Deleted = dto.Deleted };
        showEdit = true;
    }

    private Phase? deletingModel;
    private bool showConfirm;

    private void ConfirmDelete(Phase dto)
    {
        deletingModel = dto;
        showConfirm = true;
    }

    private async Task OnDeleteConfirmed()
    {
        if (deletingModel is null) return;
        deletingModel.Deleted = true;
        var ok = await Clients.UpdateAsync(deletingModel.Id, deletingModel);
        if (ok) await LoadAsync();
        deletingModel = null;

    }

    private async Task OnCreateSaved(PortalObject dto)
    {
        Phase phase = new Phase
        {
            Id = dto.Id,
            Name = dto.Name,
            Description = dto.Description,
            Deleted = dto.Deleted
        };

        var created = await Clients.CreateAsync(phase);
        if (created is not null) await LoadAsync();

    }

    private async Task OnEditSaved(PortalObject dto)
    {
        if (dto.Id == 0) return;

        Phase phase = new Phase
        {
            Id = dto.Id,
            Name = dto.Name,
            Description = dto.Description,
            Deleted = dto.Deleted
        };

        var ok = await Clients.UpdateAsync(dto.Id, phase);
        if (ok) await LoadAsync();

    }

}
